# @package _global_

defaults:
    - new_matcher_megadepth/default
    - override /model: new_matcher_two_stage
    - _self_

tags: ["train-new_matcher_two_stage-megadepth"]
model:
    loss:
        coarse_cls_sparse: False
        extra_coarse_cls_sparse: False
