# @package _global_

defaults:
  - new_matcher_megadepth/default
  - _self_

tags: ["eval-new_matcher-megadepth-ds", "bs=8-image_size=1216"]

data:
  test_dataset:
    image_size: 1216

model:
  net:
    coarse_matching:
      type: "flow"
      sparse: true  # no effect for evaluation

trainer:
  devices: 8
