# @package _global_

defaults:
  - new_matcher_megadepth/default
  - _self_

tags: ["train-new_matcher-megadepth-ds-dense", "bs=8-image_size=832"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: false

trainer:
  devices: 8
