# @package _global_

defaults:
  - new_matcher_megadepth/default
  - _self_

tags: ["train-new_matcher-megadepth-ds-sparse", "bs=8-image_size=832"]

model:
  loss:
    coarse_cls_sparse: true
    fine_cls_sparse: true

trainer:
  devices: 8
