# @package _global_

defaults:
  - new_matcher_megadepth/default
  - _self_

tags: ["train-new_matcher-megadepth-ds-sparse", "bs=8-image_size=832-samples=50"]

data:
  train_sampler:
    samples_count_per_subset: 50

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true
  canonical_warmup_step_count: 937.5

trainer:
  devices: 8
