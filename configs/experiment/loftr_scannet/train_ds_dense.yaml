# @package _global_

defaults:
  - loftr_scannet/default
  - _self_

tags: ["train-loftr-scannet-ds-dense", "bs=24"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: false

trainer:
  devices: 8
