# @package _global_

defaults:
  - loftr_scannet/default
  - _self_

tags: ["train-loftr-scannet-ot-sparse", "bs=24"]

model:
  net:
    coarse_matching:
      type: "optimal_transport"
      sparse: true

trainer:
  devices: 8
