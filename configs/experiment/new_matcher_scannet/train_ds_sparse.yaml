# @package _global_

defaults:
  - new_matcher_scannet/default
  - _self_

tags: ["train-new_matcher-scannet-ds-sparse", "bs=24"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true

trainer:
  devices: 8
