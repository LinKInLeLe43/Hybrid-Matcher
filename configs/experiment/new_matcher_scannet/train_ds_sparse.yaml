# @package _global_

defaults:
  - new_matcher_scannet/default
  - _self_

tags: ["train-new_matcher-scannet-ds-sparse"]

model:
  loss:
    coarse_cls_sparse: true
    fine_cls_sparse: true

trainer:
  devices: 8
