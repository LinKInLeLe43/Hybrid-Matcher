# @package _global_

defaults:
    - new_matcher_scannet/default
    - override /model: new_matcher_one_stage
    - _self_

tags: ["train-new_matcher_one_stage-scannet"]
model:
    loss:
        coarse_cls_sparse: False
        extra_coarse_cls_sparse: False
