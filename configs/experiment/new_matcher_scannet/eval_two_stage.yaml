# @package _global_

defaults:
  - new_matcher_scannet/default
  - override /model: new_matcher_two_stage
  - _self_

tags: ["eval-new_matcher_two_stage-scannet"]

model:
  net:
    coarse_matching:
      border_removal: 0