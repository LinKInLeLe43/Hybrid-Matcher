# @package _global_

defaults:
  - new_matcher_scannet/default
  - override /model: new_matcher_one_stage
  - _self_

tags: ["eval-new_matcher_one_stage-scannet"]

model:
  loss:
    coarse_cls_sparse: true

trainer:
  devices: 8
