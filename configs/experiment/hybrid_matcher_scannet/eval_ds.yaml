# @package _global_

defaults:
  - hybrid_matcher_scannet/default
  - _self_

tags: ["eval-hybrid_matcher-scannet-ds"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true  # no effect for evaluation
      margin_remove: 0
