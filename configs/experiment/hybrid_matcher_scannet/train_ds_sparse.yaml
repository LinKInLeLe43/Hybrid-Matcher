# @package _global_

defaults:
  - hybrid_matcher_scannet/default
  - _self_

tags: ["train-hybrid_matcher-scannet-ds-sparse"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true
