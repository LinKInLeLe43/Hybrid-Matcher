# @package _global_

defaults:
  - hybrid_matcher_megadepth/default
  - _self_

tags: ["train-hybrid_matcher-megadepth-ds-sparse", "image_size=832"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true
