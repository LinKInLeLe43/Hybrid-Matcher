# @package _global_

defaults:
  - loftr_megadepth/default
  - _self_

tags: ["train-loftr-megadepth-ds-sparse", "bs=8-image_size=840"]

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true

trainer:
  devices: 8
