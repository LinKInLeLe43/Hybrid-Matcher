# @package _global_

defaults:
  - loftr_megadepth/default
  - _self_

tags: ["train-loftr-megadepth-ot-dense", "bs=8-image_size=840"]

model:
  net:
    coarse_matching:
      type: "optimal_transport"
      sparse: false

trainer:
  devices: 8
