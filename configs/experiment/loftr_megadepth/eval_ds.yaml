# @package _global_

defaults:
  - loftr_megadepth/default
  - _self_

tags: ["eval-loftr-megadepth-ds", "bs=8-image_size=1200"]

data:
  test_dataset:
    image_size: 1200

model:
  net:
    coarse_matching:
      type: "dual_softmax"
      sparse: true  # no effect for evaluation

trainer:
  devices: 8
